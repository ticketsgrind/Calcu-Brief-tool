# Beide zijn pure Python: geen compiler nodig, en ze werken op elke Windows-laptop.
PyYAML>=6.0          # leest teksten.yaml, ondertekenaars.yaml en de offertes
Jinja2>=3.1          # vult het Word-sjabloon in

# python-docx is optioneel. tools/extract_docx.py gebruikt het als het er is en
# valt anders terug op de standaardbibliotheek; de uitvoer is gelijk.
# python-docx>=1.1.0
